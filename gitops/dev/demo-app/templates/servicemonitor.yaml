---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: demo-app-dev
  namespace: demo-app-dev
spec:
  selector:
    matchLabels:
      app: demo-app
      fuckit: ido                                                                                                                                                              │
  endpoints:
  - port: http
    interval: 30s
